---
component: code
slug: code
platform: javafx
status: stable
source: javafx/src/main/java/design/prizm/fx/controls/PrizmCode.java
---

# PrizmCode (JavaFX)

An inline code token — monospace text on a muted bordered chip (Label subclass).

In spec parity with the React `Code` (`components/ui/code.tsx`). The full
Java API (constructors, enums, methods) is in `lib/javafx-api.ts` under the
`code` key and on the components page at `/components/code`.

## Source

`javafx/src/main/java/design/prizm/fx/controls/PrizmCode.java`. Styling:
`javafx/src/main/resources/prizm/prizm.css`.
